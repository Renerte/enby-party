module.exports = {
    // mode: 'jit', // TODO: Tailwind JIT does not work with sass
    purge: ['./src/**/*.html', './src/**/*.ts', './src/**/*.vue'],
    darkMode: false, // or 'media' or 'class'
    theme: {
        extend: {},
    },
    variants: {
        extend: {},
    },
    plugins: [],
};
